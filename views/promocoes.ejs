<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Promoções - FideliSys</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<header class="bg-dark text-white text-center p-3 mb-4">
    <h1>FideliSys</h1>
    <p>Seus cartões fidelidade em um só lugar</p>
</header>

<nav class="container d-flex justify-content-center mb-4">
    <ul class="nav nav-pills">
        <li class="nav-item">
            <a class="nav-link <% if(paginaAtiva === 'cartoes') { %>active<% } %>" href="/meus-cartoes">
                <i class="bi bi-person-vcard-fill"></i> Meus Cartões
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <% if(paginaAtiva === 'promocoes') { %>active<% } %>" href="/promocoes">
                <i class="bi bi-megaphone-fill"></i> Promoções
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <% if(paginaAtiva === 'notificacoes') { %>active<% } %>" href="/notificacoes">
                <i class="bi bi-bell-fill"></i> Notificações
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <% if(paginaAtiva === 'perfil') { %>active<% } %>" href="/perfil-cliente">
                <i class="bi bi-person-circle"></i> Meu Perfil
            </a>
        </li>
    </ul>
</nav>

<main class="container">
    <h2 class="mb-4">Promoções Ativas</h2>

    <% if (promocoes && promocoes.length > 0) { %>
    <div class="row gy-4">
        <% promocoes.forEach(promo => { %>
        <div class="col-md-6">
            <div class="card promotion-card">
                <div class="card-body">
                    <h5 class="card-title"><%= promo.titulo %></h5>
                    <h6 class="card-subtitle mb-2 text-muted"><%= promo.restaurante %></h6>
                    <hr>
                    <p class="card-text"><strong>Regras:</strong> <%= promo.regras %></p>
                    <p class="card-text"><strong>Prêmio:</strong> <span class="text-success fw-bold"><%= promo.premio %></span></p>
                </div>
                <div class="card-footer text-muted">
                    Validade: <%= promo.validade %>
                </div>
            </div>
        </div>
        <% }); %>
    </div>
    <% } else { %>
    <div class="alert alert-info text-center">
        <p class="mb-0">Nenhuma promoção ativa no momento.</p>
    </div>
    <% } %>

</main>

<footer class="text-center text-muted mt-5 mb-3">
    <p>&copy; <%= new Date().getFullYear() %> FideliSys</p>
</footer>

</body>
</html>

