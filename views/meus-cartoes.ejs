<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meus Cartões - FideliSys</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="cliente.css">

    <style>
        .stamps-grid {
            display: grid;
            gap: 10px;
            margin: 20px 0;
        }

        .stamp {
            width: 100%;
            aspect-ratio: 1 / 1;
            border: 2px dashed #ced4da;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #e9ecef;
        }

        .stamp.filled {
            background-color: #198754;
            border-style: solid;
            border-color: #157347;
            color: white;
            font-size: 1.5rem;
            font-weight: bold;
        }
    </style>
</head>
<body>

<header class="bg-dark text-white text-center p-3 mb-4">
    <h1>FideliSys</h1>
    <p>Seus cartões fidelidade em um só lugar</p>
</header>

<nav class="container d-flex justify-content-center mb-4">
    <ul class="nav nav-pills">
        <li class="nav-item">
            <a class="nav-link <% if(paginaAtiva === 'cartoes') { %>active<% } %>" href="/meus-cartoes">
                <i class="bi bi-person-vcard-fill"></i> Meus Cartões
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <% if(paginaAtiva === 'promocoes') { %>active<% } %>" href="/promocoes">
                <i class="bi bi-megaphone-fill"></i> Promoções
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <% if(paginaAtiva === 'notificacoes') { %>active<% } %>" href="/notificacoes">
                <i class="bi bi-bell-fill"></i> Notificações
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link <% if(paginaAtiva === 'perfil') { %>active<% } %>" href="/perfil-cliente">
                <i class="bi bi-person-circle"></i> Meu Perfil
            </a>
        </li>
    </ul>
</nav>

<main class="container">
    <h2 class="mb-4">Meus Cartões Ativos</h2>

    <% if (cards && cards.length > 0) { %>
    <div class="row gy-4">
        <% cards.forEach(card => { %>
        <div class="col-md-6">
            <div class="card loyalty-card">
                <div class="card-body">
                    <h5 class="card-title"><%= card.restaurante %></h5>
                    <p class="card-subtitle mb-2 text-muted"><%= card.promocao %></p>
                    <hr>

                    <div class="stamps-grid" style="grid-template-columns: repeat(5, 1fr);">
                        <% for (let i = 0; i < card.registrosTotais; i++) { %>
                        <div class="stamp <%= (i < card.registrosFeitos) ? 'filled' : '' %>" title="Registro <%= i + 1 %>">
                            <% if (i < card.registrosFeitos) { %>✔<% } %>
                        </div>
                        <% } %>
                    </div>

                    <p class="text-center fw-bold fs-5">
                        Progresso: <%= card.registrosFeitos %> de <%= card.registrosTotais %>
                    </p>

                    <% if (card.registrosFeitos >= card.registrosTotais) { %>
                    <div class="alert alert-success text-center">
                        <h5 class="alert-heading">Prêmio Desbloqueado!</h5>
                        <p>Você pode resgatar seu prêmio: <strong><%= card.premio %></strong>.</p>
                        <hr>
                        <p class="mb-0">Apresente este card no caixa para validar seu resgate.</p>
                    </div>
                    <% } else { %>
                    <div class="alert alert-light text-center">
                        <strong>Prêmio:</strong> <%= card.premio %>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
        <% }); %>
    </div>
    <% } else { %>
    <div class="alert alert-info text-center">
        <p class="mb-0">Você ainda não possui cartões fidelidade ativos.</p>
    </div>
    <% } %>

</main>

<footer class="text-center text-muted mt-5 mb-3">
    <p>&copy; <%= new Date().getFullYear() %> FideliSys</p>
</footer>

</body>
</html>